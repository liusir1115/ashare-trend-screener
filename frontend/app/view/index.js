export {
  renderAdvice,
  renderBacktest,
  renderParameters,
  renderParsedRules,
  renderPlaybooks,
  renderScope,
  renderSummaryGrid,
  renderSummaryText,
  renderTopMeta,
} from "../modules/strategy/render-strategy-panel.js";

export { renderCandidates, renderResultsTable } from "../modules/candidates/render-candidates.js";
export { renderMarketReview, renderQuestionAnswer, renderSourceStatus } from "../modules/news-review/render-review.js";
